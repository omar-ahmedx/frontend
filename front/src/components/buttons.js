import React from "react";

function Button() {
  return (
    <div className="btn-container">
      {/*Unfinished because of the time */}
      <button className="preview-btn btn">Preview</button>
      <button className="send-btn btn">Send</button>
    </div>
  );
}

export default Button;
